package projet.app.ai.agent.intent;

/** High-level intent class derived deterministically from the user query. */
public enum IntentType {
    DEFINITION,
    THRESHOLD_LOOKUP,
    RATIO_VALUE,
    DASHBOARD_OVERVIEW,
    TREND_ANALYSIS,
    STRESS_TEST,
    INTERPRETATION,
    GENERAL
}
